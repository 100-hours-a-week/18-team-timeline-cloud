apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-app
  namespace: argocd
  labels:
    app.kubernetes.io/name: backend
spec:
  project: default
  source:
    repoURL: https://github.com/chang18-cloud/18-team-timeline-cloud
    targetRevision: HEAD
    path: argocd/manifests/backend
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true 